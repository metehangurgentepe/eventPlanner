//
//  Route.swift
//  EventPlanner
//
//  Created by Metehan Gürgentepe on 14.08.2023.
//
/*
import Foundation

enum Route{
    case addUser
    case editEvent(item: Event)
    case createdEvent
    case savedEvent
    case location
    case editProfile
    case profile
    case detailEvent
    case event
    case addEvent
    case register
    
}

extension Route: Hashable{
    func hash(into hasher: inout Hasher){
        hasher.combine(self.hashValue)
    }
    
    static func == (lhs: Route, rhs: Route) -> Bool {
        switch(lhs,rhs){
        case (.addUser,.addUser):
            return true
        case (.editEvent(item:), .editEvent(item: <#T##Event#>)):
            
        case (.createdEvent, _):
            <#code#>
        case (.savedEvent, _):
            <#code#>
        case (.location, _):
            <#code#>
        case (.editProfile, _):
            <#code#>
        case (.profile, _):
            <#code#>
        case (.detailEvent, _):
            <#code#>
        case (.event, _):
            <#code#>
        case (.addEvent, _):
            <#code#>
        case (.register, _):
            <#code#>
        case (_, .editEvent):
            <#code#>
        case (_, .createdEvent):
            <#code#>
        case (_, .savedEvent):
            <#code#>
        case (_, .location):
            <#code#>
        case (_, .editProfile):
            <#code#>
        case (_, .profile):
            <#code#>
        case (_, .detailEvent):
            <#code#>
        case (_, .event):
            <#code#>
        case (_, .addEvent):
            <#code#>
        case (_, .register):
            <#code#>
        }
    default:
        return false
    }
    extension Route: View{
        
    }

*/
